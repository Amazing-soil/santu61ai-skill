---
name: ai-tech-video-production
description: AI 科技中文口播短视频的端到端生产工作流。用户提供口播视频和文案后，用于先切真实时间线，再匹配素材，生成或整理 HTML/Anime.js/GPT Image/实操录屏素材，并产出可编辑 Remotion 工程、导出 MP4、检查 PIP/字幕安全区/素材转场/图层问题。适合知识型 AI 工具、AI 工作流、GEO、科技观点和产品实操类短视频。
---

# AI Tech Video Production

把「口播视频 + 文案 + 素材」做成可编辑的视频工程。这个 skill 是总控流程；素材拆解和素材规划优先调用 `ai-tech-video-assets`。

## Core Positioning

- `ai-tech-video-assets`：负责文案拆段、素材类型判断、素材规划表。
- `ai-tech-video-production`：负责真实时间线、素材匹配、剪辑工程、PIP、转场、导出和验收。

## Default Workflow

1. 获取口播视频、口播文案、已有素材目录。
2. 先切真实口播时间线，不按文案估算硬配素材。
3. 调用或复用 `ai-tech-video-assets` 的素材规划，判断每段素材类型：
   - 实操录屏/截图：优先保留真实操作，不叠复杂概念图。
   - HTML/Anime.js 动效：用于观点、趋势、对比、任务关系。
   - GPT Image 图片：用于抽象场景和情绪钩子。
   - 太短段落：宁可不用素材，也不要让素材播不完。
4. 检查素材节奏：不要整条视频都用文字信息图；每 `30 秒` 至少安排 `1` 个 GPT Image / AI 图片素材，最好 `1-2` 个，除非该 30 秒全部是真实实操录屏。
5. 生成或整理素材，统一放入工程媒体目录。
6. 生成可编辑 Remotion 工程，核心可改项进入 `src/timeline.json`。
7. 抽关键帧检查布局，重点检查顶部安全区、字幕区、正文右下 PIP，以及开头/结尾居中强化出镜的进出场，再导出完整 MP4。
8. 交付工程路径、最终 MP4、检查图和可修改说明。

## Non-Negotiable Rules

- 默认画幅：`1080x1920`，默认不额外加字幕，必须给用户后期字幕留空间。
- 如果用户要求配字幕或烧录字幕，正文字幕默认放在**中间偏下安全区**，不要贴正下方；推荐左侧/中部 `Y=1120-1360` 并避开右下真人 PIP。开头/结尾居中强化出镜时，字幕应避开人物脸部和胸口，必要时缩短或临时下移。
- 非图片素材（HTML/Anime.js 动效、文字信息图、流程卡、图表、代码/窗口模拟）的整体布局必须下移：`Y=0-260` 只放背景氛围，不放标题、卡片、图标组、流程节点等核心信息；可读核心信息优先放在 `Y=280-1080`，首个核心元素顶部不要进入 `Y=0-260`，建议 `Y>=280`。
- 正文阶段素材核心信息集中在安全的中部偏上区域，不贴近屏幕上沿，不进入中间偏下字幕区和右下真人 PIP 区；开头/结尾居中强化出镜时，素材围绕人物重新排版。
- 正文短标签默认不用发光小矩形框。对比/反常识文案优先用**分屏对照**；步骤/特征/判断标准文案优先用**HUD 坐标清单**。实现时用细线、编号、弱分割、扫描光和字重层级承载信息，避免按钮感。
- 素材类型必须混合：文字信息图、HTML/Anime 动效、GPT Image 图片、实操录屏按语义搭配；不要整条视频全是文字信息图。
- GPT Image 图片密度：每 `30 秒` 至少 `1` 个，最好 `1-2` 个；图片段不在图内生成字幕、标题、金句或假 UI。
- 真人 PIP 必须来自原口播视频，不允许素材进入 PIP 轨道。
- PIP 默认采用三阶段动态布局：开头前 `3 秒` 居中放大强化出镜，正文移动至右下小 PIP，结尾最后 `3 秒` 回到居中放大；如果真实口播钩子或收束段不足 `3 秒`，按真实边界缩短。
- 开头和结尾的居中人物不能成为唯一画面：必须保留语义相关背景素材，并搭配少量钩子词、结论词或信息标签；不要直接切成全屏纯人像。
- 居中强化出镜与右下 PIP 之间必须平滑移动和缩放，不能硬切。
- PIP 使用等比例裁切，不把 `9:16` 原片直接缩小拉伸。
- 相邻素材不要硬切闪屏，也不要双透明交叉淡化漏出底图；使用覆盖式淡入。
- MP4 动效要根据真实口播段时长变速，尽量完整播放。
- 如果口播段太短，删除该段素材或换静态图，不硬塞完整动效。

## When To Read References

- 生成 Remotion 工程或修改模板：读 [references/remotion-rules.md](references/remotion-rules.md)。
- 做时间线和素材匹配：读 [references/timeline-workflow.md](references/timeline-workflow.md)。
- 做交付前验收：读 [references/quality-checks.md](references/quality-checks.md)。

## Required Outputs

根据任务阶段至少输出以下之一：

- 时间线与素材匹配表
- 素材生成/整理目录
- 可编辑 Remotion 工程
- 最终 MP4
- 关键帧检查图
- 剪辑师交付说明

## Do Not

- 不要直接把最终 MP4 当成唯一交付物；必须保留可编辑工程或可编辑说明。
- 不要先剪再猜文案；必须先确认或切分时间线。
- 不要让素材切换中间露出口播底图。
- 不要把字幕安全区理解成不能铺背景；背景可以铺满，但核心信息必须避让。
